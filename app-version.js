// @ts-check
//-----------------------------------------------------------------------
// APP VERSION
// Single runtime source for the release label. bump-version.sh updates
// this file together with VERSION and shared-header.js.
//-----------------------------------------------------------------------

const AppVersion = Object.freeze({
    current: '235'
});

window.AppVersion = AppVersion;
