{
  "_note": "API keys are managed via the UI and stored in localStorage. No config file needed for API keys.",
  "_api_keys": {
    "claude": "Managed in player.html Settings - stored in localStorage as 'claudeApiKey'",
    "openai": "Managed in ebook.html Settings - stored in localStorage as 'openaiApiKey'"
  },
  "deploy": {
    "user": "ernop",
    "host": "fuseki.net",
    "remotePath": "/home/ernop/fuseki.net/public/music8899b",
    "publicUrl": "https://fuseki.net/music8899b/player.html"
  }
}